name: Android CI
on:
  push:
    branches:
      - release/*
  workflow_dispatch:

permissions:
  contents: write

env:
  JAVA_VERSION: '17'
  JAVA_DIST: 'zulu'
  BUILD_TOOLS_VERSION: '34.0.0'
  GRADLE_CACHE_PATHS: |
    ~/.gradle/caches
    ~/.gradle/wrapper

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.get_version_name.outputs.version_name }}
      gradle_cache_key: ${{ steps.cache_gradle.outputs.cache-hit }}
    steps:
      - uses: actions/checkout@v3

      - name: Set Up JDK
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DIST }}
          cache: 'gradle'

      - name: Cache Gradle
        id: cache_gradle
        uses: actions/cache@v3
        with:
          path: ${{ env.GRADLE_CACHE_PATHS }}
          key: ${{ runner.os }}-${{ hashFiles('**/*.gradle*') }}-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('**/buildSrc/**/*.kt') }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Get Version Name
        id: get_version_name
        run: |
          VERSION_NAME=$(${{github.workspace}}/gradlew -q printVersionName)
          echo "version_name={VERSION_NAME}" >> $GITHUB_OUTPUT

  lint:
    needs: [ setup ]
    name: Perform Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Restore Gradle cache
        uses: actions/cache@v3
        with:
          path: ${{ env.GRADLE_CACHE_PATHS }}
          key: ${{ needs.setup.outputs.gradle_cache_key || runner.os }}-${{ hashFiles('**/*.gradle*') }}-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('**/buildSrc/**/*.kt') }}

      - name: Set Up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DIST }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run lint
        run: ./gradlew lintDebug

      - name: Upload html test report
        uses: actions/upload-artifact@v3
        with:
          name: lint.html
          path: app/build/reports/lint-results-debug.html

  debug-apk:
    needs: [ lint, setup ]
    name: Generate Debug APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Restore Gradle cache
        uses: actions/cache@v3
        with:
          path: ${{ env.GRADLE_CACHE_PATHS }}
          key: ${{ needs.setup.outputs.gradle_cache_key || runner.os }}-${{ hashFiles('**/*.gradle*') }}-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('**/buildSrc/**/*.kt') }}

      - name: Set Up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DIST }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Generate file name env var
        run: |
          VERSION_NAME=${{ needs.setup.outputs.version_name }}
          MESSAGE="nutriLog-${VERSION_NAME}-debug"
          echo "OUTPUT_NAME=${MESSAGE}" >> $GITHUB_ENV

      - name: Run gradle tests
        run: ./gradlew test

      - name: Build with Gradle
        run: ./gradlew build

      - name: Build APK
        run: bash ./gradlew assembleDebug --stacktrace

      - name: Move Signed APK
        run: mv app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/${{ env.OUTPUT_NAME }}.apk

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.OUTPUT_NAME }}
          path: app/build/outputs/apk/debug/${{ env.OUTPUT_NAME }}.apk

  build-release:
    needs: [ lint, setup ]
    name: Build Release signed APK and Bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Restore Gradle cache
        uses: actions/cache@v3
        with:
          path: ${{ env.GRADLE_CACHE_PATHS }}
          key: ${{ needs.setup.outputs.gradle_cache_key || runner.os }}-${{ hashFiles('**/*.gradle*') }}-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('**/buildSrc/**/*.kt') }}

      - name: Set Up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DIST }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Generate file name env var
        run: |
          VERSION_NAME=${{ needs.setup.outputs.version_name }}
          MESSAGE="nutriLog-${VERSION_NAME}-release"
          echo "OUTPUT_NAME=${MESSAGE}" >> $GITHUB_ENV

      - name: Run gradle tests
        run: ./gradlew test

      - name: Build with Gradle
        run: ./gradlew build

      - name: Build APK
        run: bash ./gradlew assembleRelease --stacktrace

      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        id: sign_app_apk
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: ${{ env.BUILD_TOOLS_VERSION }}

      - name: Move Signed APK
        run: mv ${{steps.sign_app_apk.outputs.signedReleaseFile}} app/build/outputs/apk/release/${{ env.OUTPUT_NAME }}.apk

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.OUTPUT_NAME }}
          path: app/build/outputs/apk/release/${{ env.OUTPUT_NAME }}.apk

      - name: Build release AAB
        run: bash ./gradlew bundleRelease --stacktrace

      - name: Sign Bundle
        uses: r0adkll/sign-android-release@v1
        id: sign_app_bundle
        with:
          releaseDirectory: app/build/outputs/bundle/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: ${{ env.BUILD_TOOLS_VERSION }}

      - name: Move Signed Bundle
        run: mv ${{steps.sign_app_bundle.outputs.signedReleaseFile}} app/build/outputs/bundle/release/${{ env.OUTPUT_NAME }}.aab

      - name: Upload Signed AAB
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.OUTPUT_NAME }}
          path: app/build/outputs/bundle/release/${{ env.OUTPUT_NAME }}.aab

  release:
    name: Create Release
    needs: [ build-release, debug-apk, lint, setup ]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Restore Gradle cache
        uses: actions/cache@v3
        with:
          path: ${{ env.GRADLE_CACHE_PATHS }}
          key: ${{ needs.setup.outputs.gradle_cache_key || runner.os }}-${{ hashFiles('**/*.gradle*') }}-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}-${{ hashFiles('**/buildSrc/**/*.kt') }}

      - name: Set Up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DIST }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Generate tag name and release name env var
        run: |
          VERSION_NAME=${{ needs.setup.outputs.version_name }}
          RELEASE_MESSAGE="Release v${VERSION_NAME}"
          TAG_MESSAGE="release-${VERSION_NAME}"
          OUTPUT_NAME="nutriLog-${VERSION_NAME}-release"
          echo "TAG_NAME=${TAG_MESSAGE}" >> $GITHUB_ENV
          echo "RELEASE_NAME=${RELEASE_MESSAGE}" >> $GITHUB_ENV
          echo "OUTPUT_NAME=${OUTPUT_NAME}" >> $GITHUB_ENV

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.TAG_NAME }}
          release_name: ${{ env.RELEASE_NAME }}

      - name: Download Artifact
        uses: actions/download-artifact@v3
        with:
          name: ${{ env.OUTPUT_NAME }}
          path: ./artifacts

      - name: Unzip Artifact
        run: unzip ./artifacts/${{ env.OUTPUT_NAME }}.zip -d ./artifacts

      - name: Upload Release APK and Bundle
        uses: meeDamian/github-release@2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ env.TAG_NAME }}
          name: ${{ env.RELEASE_NAME }}
          draft: false
          prerelease: false
          files: |
            ./artifacts/*.apk
            ./artifacts/*.aab
          allow_override: true
